# Domain Docs

This repo uses a single-context layout.

Canonical context files:

- `CONTEXT/CONTEXT.md`
- `CONTEXT/CONTEXT-MAP.md`
- `CONTEXT/docs/adr/`

Skills should read and update these paths instead of root-level `CONTEXT.md`, root-level `CONTEXT-MAP.md`, or root-level `docs/adr/`.
