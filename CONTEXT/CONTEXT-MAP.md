# Context Map

This repo currently uses a single context:

- `CONTEXT/CONTEXT.md`

If the repo becomes a monorepo or grows multiple independent domains, add each context path here and keep shared agent docs under `CONTEXT/docs/agents/`.
