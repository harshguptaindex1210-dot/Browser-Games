# Handoff — 2026-07-06 1115

## Objective
Plan v1 of a CrazyGames-style browser-games portal (anonymous, self-hosted games, single-player-first, one multiplayer game). `/part1` planning chain — planning artifacts only, no application code.

## Repo / branch / remote
- Repo: `C:\Users\Harsh\Desktop\Work\Browser-Games`
- Remote: `https://github.com/harshguptaindex1210-dot/Browser-Games.git`
- Branch: `main`
- Last commit before this handoff: `a06258f` (docs: /part1 planning)

## What changed in this session (continuation)
- Added a **Communication style** section to `CONTEXT/AGENTS.md`: the user has no coding experience and limited English, so all chat with the user must use plain words, short sentences, no jargon, no unrequested code blocks. Technical docs and code comments keep normal language; this rule is chat-only.
- Installed **GitHub CLI** (`winget install GitHub.cli`, v2.96.0). Authenticated as `harshguptaindex1210-dot` via the user's `gh auth login`.
- Filed all 7 issues to the GitHub issue tracker with the `ready-for-agent` label.
- Updated each issue's "Blocked by" section on GitHub to reference real issue numbers.

## Issue numbers (the mapping that matters)
| Slice | Title | Issue | Blocked by |
|-------|-------|-------|------------|
| S1 | Foundation tracer: one stub game end-to-end | https://github.com/harshguptaindex1210-dot/Browser-Games/issues/6 | None — START HERE |
| S2 | Catalog browsing: tags + homepage rows | #3 | #6 (S1) |
| S3 | Free-text search | #1 | #3 (S2) |
| S4 | Player Page affordances: favorites, share, mute, fullscreen | #7 | #6 (S1) |
| S5 | Ad Slot (AdSense + house ad fallback) | #5 | #6 (S1) |
| S6 | Multiplayer game via PartyKit | #2 | #6 (S1) |
| S7 | SEO + a second real game | #4 | #3 (S2), #1 (S3) |

Issue numbers are out of slice order because PowerShell's hashtable didn't preserve insertion order at filing time. The **slice name** (S1..S7) is the canonical ordering; the issue number is just the GitHub handle. Always cross-reference by slice name in chat.

## Commands already run and outcomes
- `winget install --id GitHub.cli` — installed gh 2.96.0.
- `git config user.name "Harsh"` + `git config user.email "harshguptaindex1210-dot@users.noreply.github.com"` — repo-local git identity set (per user's explicit request).
- `git commit -m "docs: /part1 planning - PRD, ADR-0001, invariants, 7 issues, handoff"` → commit `a06258f`, pushed to `origin/main` successfully.
- `gh auth login` — completed by the user; auth status confirmed.
- `gh label create ready-for-agent` — label created.
- `gh issue create` × 7 — all 7 issues filed (#1–#7).
- `gh issue edit` × 7 — Blocked-by sections updated with real #refs.

## What remains to do next
- **START with issue #6 (S1 — Foundation tracer).** It is the only unblocked issue. S2–S6 all block on it; S7 blocks on S2+S3.
- Run `/part2` to implement S1 test-first, red-team it, and push a handoff.

## Known blockers / risks / assumptions
- **None blocking.** gh is installed and authed; git identity is set; issues are filed with the `ready-for-agent` label; planning docs are pushed.
- **Assumption — AdSense as the ad provider.** Real AdSense fill is out of test scope (INV-F1 only tests the no-fill fallback). AdSense site approval may block live ads even after S5 ships; the house-ad fallback keeps the layout intact.
- **Assumption — Vercel single region.** INV-F6 (multi-region failover) is documented out of scope for v1.
- **Assumption — Desktop-first.** Mobile gameplay is best-effort; desktop is the supported target.
- **Risk — `allow-scripts` without `allow-same-origin`.** Games must use the Game Adapter (postMessage) for saves, NOT localStorage inside the iframe. Documented in S1 brief and ADR-0001; S1 contract tests must enforce it.
- **Communication style rule is now live.** Every future agent run in this repo must follow `CONTEXT/AGENTS.md` § Communication style — plain words, short sentences, no jargon in chat with the user.

## Suggested skills for the next agent
- `/part2` — implement issue #6 (S1) test-first, red-team it, push a handoff.
- `tdd` — S1 is test-heavy (Game Adapter postMessage contract as the single high seam); build red-green.
- `prototype` — if the Game Adapter contract feels uncertain, prototype it before S1 implementation.

## Pointers (do not duplicate — read these)
- PRD: `CONTEXT/docs/prd/0001-browser-games-v1.md`
- ADR: `CONTEXT/docs/adr/0001-browser-games-v1-architecture.md`
- Invariants + decisions + ubiquitous language: `CONTEXT/CONTEXT.md`
- Issues (local copies, synced with GitHub): `CONTEXT/docs/issues/S1-foundation-tracer.md` … `S7-seo-second-game.md`
- Communication style rule: `CONTEXT/AGENTS.md` § Communication style
