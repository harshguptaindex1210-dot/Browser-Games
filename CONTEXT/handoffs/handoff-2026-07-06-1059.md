# Handoff — 2026-07-06 1059

## Objective
Plan the v1 of a CrazyGames-style browser-games portal (anonymous, self-hosted games, single-player-first, one multiplayer game). `/part1` planning chain — produces planning artifacts only, no application code.

## Repo / branch / remote
- Repo: `C:\Users\Harsh\Desktop\Work\Browser-Games`
- Remote: `https://github.com/harshguptaindex1210-dot/Browser-Games.git`
- Branch: `main`
- Base commit before this session: `c03b721` (Initial commit)

## What changed in this session
- `CONTEXT/CONTEXT.md` — wrote project description, ubiquitous language, 11 current decisions (D1–D11), and locked invariants (INV-P1..P5, INV-F1..F6, INV-S1..S7).
- `CONTEXT/docs/adr/0001-browser-games-v1-architecture.md` — ADR capturing the v1 architecture.
- `CONTEXT/docs/prd/0001-browser-games-v1.md` — full PRD (problem, solution, 28 user stories, implementation/testing decisions, out-of-scope).
- `CONTEXT/docs/issues/S1..S7` — 7 dependency-ordered tracer-bullet issues, each with acceptance criteria restating the relevant invariants and a machine-checkable `Verification-command`.
- `CONTEXT/handoffs/handoff-2026-07-06-1059.md` — this file.

## Commands run and outcomes
- `git remote -v` / `git branch` / `git log --oneline -5` — confirmed single `main` branch, one initial commit, GitHub remote configured.
- `gh repo view` / `gh label list` — **`gh` CLI is not installed.** PRD and issues could not be published to the GitHub issue tracker; they were written as versioned files under `CONTEXT/docs/` instead. See blocker.

## Slice order (next issue to start = S1)
1. **S1** — Foundation tracer: one stub game end-to-end. **← START HERE.** Blocked by: none.
2. **S2** — Catalog browsing: tags + homepage rows. Blocked by: S1.
3. **S3** — Free-text search. Blocked by: S2.
4. **S4** — Player Page affordances: favorites, share, mute, fullscreen. Blocked by: S1.
5. **S5** — Ad Slot (AdSense + house ad fallback). Blocked by: S1.
6. **S6** — Multiplayer game via PartyKit. Blocked by: S1.
7. **S7** — SEO + a second real game. Blocked by: S2, S3.

S2–S6 fan out from S1; S7 depends on S2+S3. S1 is the only unblocked issue.

## Known blockers / risks / assumptions
- **BLOCKER — `gh` CLI missing.** PRD + 7 issues were written to `CONTEXT/docs/` as markdown files instead of being filed on the GitHub issue tracker. Recovery step: install GitHub CLI (`winget install --id GitHub.cli`), `gh auth login`, then either file each issue from `CONTEXT/docs/issues/S*.md` manually or have an agent do it. The `ready-for-agent` triage label could not be applied; once filed, apply it. Until filed, `/part2` should treat `CONTEXT/docs/issues/S1-foundation-tracer.md` as the de-facto S1 brief.
- **Assumption — AdSense as the ad provider.** User asked for "free ad software that pays us to show ads"; AdSense is the recommended default. Real AdSense fill is out of test scope (INV-F1 only tests the no-fill fallback).
- **Assumption — Vercel single region.** INV-F6 (multi-region failover) is documented as out of scope for v1.
- **Assumption — Desktop-first.** Mobile gameplay is best-effort; desktop is the supported target.
- **Risk — AdSense approval.** Real-world AdSense approval requires site review and may block the live ad path even after S5 ships; the house-ad fallback keeps the layout intact in the meantime.
- **Risk — `allow-scripts` without `allow-same-origin`.** Games must use the Game Adapter (postMessage) for saves, NOT localStorage inside the iframe. This is documented in the S1 brief and ADR-0001; the S1 contract tests must enforce it.

## Suggested skills for the next agent
- `/part2` — to implement S1 (the next unblocked issue) test-first, red-team it, and push a handoff.
- `tdd` — S1 is test-heavy (Game Adapter contract as the single high seam); build red-green.
- `prototype` — if the Game Adapter postMessage contract feels uncertain, prototype it before S1 implementation.

## Pointers (do not duplicate — read these)
- PRD: `CONTEXT/docs/prd/0001-browser-games-v1.md`
- ADR: `CONTEXT/docs/adr/0001-browser-games-v1-architecture.md`
- Invariants + decisions + ubiquitous language: `CONTEXT/CONTEXT.md`
- Issues: `CONTEXT/docs/issues/S1-foundation-tracer.md` … `S7-seo-second-game.md`
